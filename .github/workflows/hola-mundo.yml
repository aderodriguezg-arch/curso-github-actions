name: CI-workflow
on: 
  push:
    branches: [ "curso-github-actions" ]

  workflow_dispatch:

jobs:
  workflow-ejemplo:
    runs-on: ubuntu-latest
    steps:
      - name: hola mundo
        run: |
            python bienvenidos.py
